<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Avantto · Interior Condition Report · Phenom 300</title>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@tabler/icons-webfont@3.19.0/dist/tabler-icons.min.css">
<style>
@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,600;0,700;1,400&family=Poppins:wght@300;400;500;600&display=swap');
*{box-sizing:border-box;margin:0;padding:0;}
:root{
  --deep:#3C3440;--deeper:#29252D;--inst:#4F556A;
  --clear:#F1ECE0;--prem:#E9A97A;--prem2:#B9997D;
  --bold:#CE5047;--mist:#BDCCD4;--white:#FFFFFF;
  --ai:#5DCAA5;--ai-dim:rgba(93,202,165,.12);--ai-border:rgba(93,202,165,.32);
}
.av{font-family:'Poppins',sans-serif;background:var(--deep);color:var(--clear);padding-bottom:60px;min-height:100vh;}

/* TOP BAR */
.av-bar{background:var(--deeper);padding:0 28px;display:flex;align-items:stretch;justify-content:space-between;border-bottom:1px solid rgba(233,169,122,.15);}
.av-bar-brand{display:flex;align-items:center;gap:16px;padding:12px 0;}
.av-logo-wrap{height:36px;display:flex;align-items:center;}
.av-logo-wrap img{height:32px;width:auto;}
.av-brand-sub{font-size:9px;letter-spacing:3px;color:var(--prem);text-transform:uppercase;border-left:1px solid rgba(233,169,122,.2);padding-left:16px;margin-left:4px;}
.av-bar-right{display:flex;align-items:center;gap:10px;flex-wrap:wrap;}
.av-chip{font-size:10px;letter-spacing:1.5px;padding:5px 12px;border:1px solid rgba(233,169,122,.3);color:var(--prem);background:rgba(233,169,122,.07);border-radius:2px;font-weight:500;}
.av-chip.ai-chip{border-color:var(--ai-border);color:var(--ai);background:var(--ai-dim);display:flex;align-items:center;gap:5px;}
.ai-dot{width:6px;height:6px;border-radius:50%;background:var(--ai);animation:pulse 2s infinite;}
@keyframes pulse{0%,100%{opacity:1;}50%{opacity:.3};}
.av-prog{height:2px;background:rgba(255,255,255,.06);}
.av-prog-fill{height:100%;background:var(--prem);transition:width .4s ease;width:0%;}

/* META */
.av-meta{display:grid;grid-template-columns:repeat(4,1fr);border-bottom:1px solid rgba(255,255,255,.06);}
.av-meta-cell{padding:16px 24px;border-right:1px solid rgba(255,255,255,.06);}
.av-meta-cell:last-child{border-right:none;}
.av-meta-lbl{font-size:9px;letter-spacing:2px;color:var(--prem2);text-transform:uppercase;margin-bottom:6px;}
.av-meta-in{font-size:13px;font-weight:500;color:var(--clear);background:transparent;border:none;border-bottom:1px solid rgba(255,255,255,.12);padding-bottom:3px;width:100%;font-family:'Poppins',sans-serif;outline:none;}
.av-meta-in:focus{border-bottom-color:var(--prem);}

/* BODY */
.av-body{padding:0 28px;}
.av-ai-info{background:var(--ai-dim);border:1px solid var(--ai-border);border-radius:2px;padding:10px 16px;margin:20px 0 0;display:flex;align-items:flex-start;gap:10px;}
.av-ai-info i{font-size:14px;color:var(--ai);flex-shrink:0;margin-top:2px;}
.av-ai-info p{font-size:10px;color:rgba(93,202,165,.85);line-height:1.6;letter-spacing:.3px;}

/* ZONE */
.av-zone{margin-top:28px;border:1px solid rgba(255,255,255,.08);background:rgba(41,37,45,.6);}
.av-zone-hdr{padding:13px 24px;display:flex;align-items:center;gap:16px;border-bottom:1px solid rgba(255,255,255,.06);position:relative;}
.av-zone-hdr::before{content:'';position:absolute;left:0;top:0;bottom:0;width:3px;background:var(--prem);}
.av-zone-num{font-family:'Playfair Display',serif;font-size:11px;color:var(--prem);letter-spacing:2px;}
.av-zone-name{font-family:'Playfair Display',serif;font-size:15px;font-weight:600;color:var(--white);}
.av-zone-badge{margin-left:auto;font-size:10px;color:var(--prem2);letter-spacing:1px;}
.av-zone-badge span{font-family:'Playfair Display',serif;font-size:16px;color:var(--prem);}
.av-col-hdr{display:grid;grid-template-columns:1fr 1fr;border-bottom:1px solid rgba(255,255,255,.06);}
.av-col-t{padding:8px 24px;font-size:9px;letter-spacing:2px;color:var(--inst);text-transform:uppercase;font-weight:600;}
.av-col-t:first-child{border-right:1px solid rgba(255,255,255,.06);}
.av-col-t.ideal{color:var(--prem2);}
.av-col-t.ideal::before{content:'— ';}

/* ITEM */
.av-item{border-bottom:1px solid rgba(255,255,255,.05);}
.av-item:last-child{border-bottom:none;}
.av-item-grid{display:grid;grid-template-columns:1fr 1fr;}
.av-item-l{padding:18px 24px;border-right:1px solid rgba(255,255,255,.05);}
.av-item-r{padding:18px 24px;background:rgba(29,25,33,.4);}
.av-item-name{font-family:'Playfair Display',serif;font-size:13px;font-weight:600;color:var(--white);margin-bottom:14px;}

/* SCORES */
.av-scores{display:flex;gap:5px;margin-bottom:14px;}
.av-sb{flex:1;padding:8px 3px;border:1px solid rgba(255,255,255,.1);background:transparent;cursor:pointer;font-family:'Poppins',sans-serif;font-size:10px;color:rgba(241,236,224,.4);transition:all .15s;text-align:center;line-height:1.4;border-radius:2px;}
.av-sb:hover{border-color:var(--prem);color:var(--prem);}
.av-sb.s1{background:rgba(206,80,71,.2);border-color:var(--bold);color:#f4a09a;}
.av-sb.s2{background:rgba(206,80,71,.1);border-color:rgba(206,80,71,.5);color:#e09090;}
.av-sb.s3{background:rgba(185,153,125,.15);border-color:var(--prem2);color:var(--prem2);}
.av-sb.s4{background:rgba(233,169,122,.15);border-color:var(--prem);color:var(--prem);}
.av-sb.s5{background:rgba(189,204,212,.15);border-color:var(--mist);color:var(--mist);}

/* PHOTO SECTION LABEL */
.av-ref-lbl{font-size:9px;letter-spacing:2px;text-transform:uppercase;margin-bottom:8px;display:flex;align-items:center;gap:6px;}
.av-ref-lbl::before{content:'';width:16px;height:1px;background:currentColor;opacity:.5;}
.av-ref-lbl.atual{color:var(--prem2);}
.av-ref-lbl.ideal{color:var(--mist);}

/* PHOTO GALLERY */
.av-gallery{display:grid;grid-template-columns:repeat(4,1fr);gap:5px;margin-bottom:10px;}
.av-thumb{position:relative;aspect-ratio:1;border-radius:2px;overflow:hidden;border:1px dashed rgba(233,169,122,.2);background:rgba(233,169,122,.03);cursor:pointer;transition:all .15s;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:3px;}
.av-thumb:hover{border-color:var(--prem);background:rgba(233,169,122,.08);}
.av-thumb.filled{border-style:solid;border-color:rgba(233,169,122,.3);cursor:default;}
.av-thumb.add-btn{border-color:rgba(93,202,165,.25);background:rgba(93,202,165,.04);}
.av-thumb.add-btn:hover{border-color:var(--ai);background:rgba(93,202,165,.1);}
.av-thumb img{width:100%;height:100%;object-fit:cover;display:block;}
.av-thumb .thumb-lbl{font-size:8px;letter-spacing:1px;color:rgba(233,169,122,.35);text-transform:uppercase;text-align:center;}
.av-thumb .thumb-lbl-ai{font-size:7px;letter-spacing:.5px;color:rgba(93,202,165,.4);text-align:center;}
.av-thumb i{font-size:16px;color:rgba(233,169,122,.3);}
.av-thumb.add-btn i{color:rgba(93,202,165,.5);}
.av-thumb .thumb-del{position:absolute;top:3px;right:3px;width:18px;height:18px;background:rgba(206,80,71,.85);border-radius:1px;display:none;align-items:center;justify-content:center;cursor:pointer;}
.av-thumb.filled:hover .thumb-del{display:flex;}
.av-thumb .thumb-del i{font-size:10px;color:white;}
.av-thumb .thumb-score{position:absolute;bottom:0;left:0;right:0;padding:2px 4px;font-size:8px;font-weight:600;text-align:center;letter-spacing:.5px;}
.av-thumb .thumb-score.s1{background:rgba(206,80,71,.85);color:#f4a09a;}
.av-thumb .thumb-score.s2{background:rgba(206,80,71,.6);color:#e09090;}
.av-thumb .thumb-score.s3{background:rgba(185,153,125,.8);color:var(--prem2);}
.av-thumb .thumb-score.s4{background:rgba(233,169,122,.7);color:var(--deeper);}
.av-thumb .thumb-score.s5{background:rgba(189,204,212,.7);color:var(--deeper);}
.av-thumb .thumb-spin{position:absolute;inset:0;background:rgba(29,25,33,.7);display:flex;align-items:center;justify-content:center;}
.av-thumb .thumb-spin i{font-size:18px;color:var(--ai);animation:spin .8s linear infinite;}
@keyframes spin{from{transform:rotate(0)}to{transform:rotate(360deg)};}

/* AI RESULT PANEL */
.av-ai-result{display:none;padding:10px 12px;background:rgba(29,25,33,.65);border:1px solid var(--ai-border);border-radius:2px;margin-top:6px;}
.av-ai-result.visible{display:block;}
.av-ai-result-hdr{display:flex;align-items:center;justify-content:space-between;margin-bottom:6px;}
.av-ai-result-hdr-left{display:flex;align-items:center;gap:6px;}
.av-ai-result-hdr-left i{font-size:11px;color:var(--ai);}
.av-ai-result-hdr-left span{font-size:9px;letter-spacing:2px;text-transform:uppercase;color:var(--ai);}
.av-ai-photos-count{font-size:9px;color:rgba(93,202,165,.5);letter-spacing:1px;}
.av-ai-result-text{font-size:11px;color:var(--clear);line-height:1.6;opacity:.85;}
.av-ai-score-row{display:flex;align-items:center;gap:10px;margin-top:6px;padding-top:6px;border-top:1px solid rgba(255,255,255,.06);}
.av-ai-score-lbl{font-size:9px;letter-spacing:1.5px;color:var(--inst);text-transform:uppercase;}
.av-ai-score-num{font-family:'Playfair Display',serif;font-size:16px;color:var(--prem);}
.av-ai-score-cat{font-size:10px;color:var(--prem2);}
.av-ai-reanalyze{margin-left:auto;font-size:9px;letter-spacing:1px;color:rgba(93,202,165,.6);cursor:pointer;padding:3px 8px;border:1px solid rgba(93,202,165,.2);border-radius:2px;text-transform:uppercase;transition:all .15s;}
.av-ai-reanalyze:hover{color:var(--ai);border-color:var(--ai-border);}
.av-ai-analyzing{display:none;align-items:center;gap:8px;padding:8px 10px;background:var(--ai-dim);border:1px solid var(--ai-border);border-radius:2px;margin-top:6px;}
.av-ai-analyzing.visible{display:flex;}
.av-ai-analyzing i{font-size:13px;color:var(--ai);animation:spin .8s linear infinite;}
.av-ai-analyzing span{font-size:10px;color:var(--ai);}

/* OBS */
.av-obs{width:100%;background:rgba(255,255,255,.04);border:1px solid rgba(255,255,255,.08);color:var(--clear);font-family:'Poppins',sans-serif;font-size:11px;padding:8px 12px;resize:none;min-height:40px;outline:none;border-radius:2px;margin-top:8px;}
.av-obs::placeholder{color:rgba(241,236,224,.2);font-size:10px;}
.av-obs:focus{border-color:rgba(233,169,122,.3);}

/* REFERENCE PANEL */
.av-ref-img-wrap{position:relative;overflow:hidden;border-radius:2px;margin-bottom:10px;}
.av-ref-img-wrap img{width:100%;height:130px;object-fit:cover;display:block;filter:saturate(0.85) brightness(0.9);}
.av-ref-badge{position:absolute;bottom:0;left:0;right:0;padding:6px 10px;background:rgba(29,25,33,.75);display:flex;align-items:center;justify-content:space-between;}
.av-ref-badge-label{font-size:9px;letter-spacing:1.5px;color:var(--prem);text-transform:uppercase;font-weight:500;}
.av-ref-badge-std{font-size:9px;color:rgba(189,204,212,.7);}
.av-ref-corner{position:absolute;top:8px;right:8px;width:14px;height:14px;border-top:1.5px solid rgba(233,169,122,.5);border-right:1.5px solid rgba(233,169,122,.5);}
.av-std-text{font-size:10px;color:rgba(189,204,212,.65);line-height:1.6;letter-spacing:.3px;font-style:italic;padding-top:6px;border-top:1px solid rgba(255,255,255,.06);}
.av-zone-ftr{background:rgba(29,25,33,.5);padding:10px 24px;display:flex;align-items:center;justify-content:space-between;border-top:1px solid rgba(255,255,255,.05);}
.av-zone-ftr-lbl{font-size:9px;letter-spacing:2px;color:var(--inst);text-transform:uppercase;}
.av-zone-ftr-val{font-family:'Playfair Display',serif;font-size:14px;color:var(--prem);}

/* DISCREPÂNCIAS */
.av-disc{margin-top:28px;border:1px solid rgba(206,80,71,.2);}
.av-disc-hdr{padding:12px 24px;background:rgba(206,80,71,.07);border-bottom:1px solid rgba(206,80,71,.12);display:flex;align-items:center;gap:10px;position:relative;}
.av-disc-hdr::before{content:'';position:absolute;left:0;top:0;bottom:0;width:3px;background:var(--bold);}
.av-disc-title{font-family:'Playfair Display',serif;font-size:13px;color:var(--bold);}
.av-disc-body{padding:20px 24px;}
.av-disc-ta{width:100%;background:rgba(255,255,255,.03);border:1px solid rgba(255,255,255,.08);color:var(--clear);font-family:'Poppins',sans-serif;font-size:12px;padding:12px 14px;resize:none;min-height:72px;outline:none;border-radius:2px;}
.av-disc-ta:focus{border-color:rgba(206,80,71,.3);}
.av-actions{display:flex;gap:8px;margin-top:14px;flex-wrap:wrap;}
.av-act{display:flex;align-items:center;gap:7px;border:1px solid rgba(255,255,255,.1);padding:8px 14px;cursor:pointer;font-size:10px;letter-spacing:1px;color:rgba(241,236,224,.45);font-weight:500;transition:all .15s;border-radius:2px;user-select:none;text-transform:uppercase;}
.av-act:hover{border-color:var(--prem);color:var(--prem);}
.av-act.on{background:rgba(233,169,122,.1);border-color:var(--prem);color:var(--prem);}
.av-act .dot{width:6px;height:6px;border-radius:50%;border:1.5px solid currentColor;flex-shrink:0;}
.av-act.on .dot{background:var(--prem);}

/* SIGN */
.av-sign{margin-top:28px;border:1px solid rgba(255,255,255,.08);}
.av-sign-hdr{padding:12px 24px;background:rgba(29,25,33,.6);border-bottom:1px solid rgba(255,255,255,.06);}
.av-sign-title{font-size:9px;letter-spacing:3px;text-transform:uppercase;color:var(--prem2);font-weight:600;}
.av-sign-body{padding:20px 24px;display:grid;grid-template-columns:1fr 1fr;gap:16px;}
.av-sign-fld label{font-size:9px;letter-spacing:2px;text-transform:uppercase;color:var(--inst);display:block;margin-bottom:7px;}
.av-sign-in{width:100%;background:transparent;border:none;border-bottom:1px solid rgba(255,255,255,.12);color:var(--clear);font-family:'Poppins',sans-serif;font-size:13px;padding:6px 0;outline:none;}
.av-sign-in:focus{border-bottom-color:var(--prem);}
.av-canvas-wrap{grid-column:span 2;border:1px dashed rgba(255,255,255,.1);background:rgba(29,25,33,.5);height:76px;position:relative;overflow:hidden;border-radius:2px;cursor:crosshair;}
.av-canvas-wrap canvas{width:100%;height:100%;display:block;}
.av-canvas-ph{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;font-size:10px;letter-spacing:2px;color:rgba(241,236,224,.12);pointer-events:none;text-transform:uppercase;}
.av-canvas-clr{position:absolute;top:7px;right:9px;font-size:9px;letter-spacing:1px;color:var(--prem2);cursor:pointer;background:rgba(60,52,64,.9);padding:3px 9px;border:1px solid rgba(233,169,122,.15);border-radius:2px;text-transform:uppercase;}

/* FOOTER */
.av-footer{margin-top:28px;padding:20px 0;display:flex;align-items:center;justify-content:space-between;border-top:1px solid rgba(255,255,255,.06);}
.av-summary{display:flex;gap:24px;}
.av-sum-item{text-align:center;}
.av-sum-val{font-family:'Playfair Display',serif;font-size:28px;color:var(--prem);display:block;}
.av-sum-lbl{font-size:9px;letter-spacing:2px;color:var(--inst);text-transform:uppercase;}
.av-sum-cls{font-family:'Playfair Display',serif;font-size:13px;font-style:italic;margin-top:3px;}
.av-footer-btns{display:flex;gap:8px;}
.av-submit{background:var(--prem);color:var(--deeper);border:none;padding:13px 28px;font-family:'Poppins',sans-serif;font-size:11px;font-weight:600;letter-spacing:2px;text-transform:uppercase;cursor:pointer;transition:all .2s;display:flex;align-items:center;gap:8px;border-radius:2px;}
.av-submit:hover{background:var(--white);transform:translateY(-1px);}
.av-submit.sec{background:rgba(93,202,165,.12);color:var(--ai);border:1px solid var(--ai-border);}
.av-submit.sec:hover{background:rgba(93,202,165,.2);}
</style>
</head>
<body>
<div class="av">
  <div class="av-bar">
    <div class="av-bar-brand">
      <div class="av-logo-wrap"><img id="av-logo" alt="Avantto" src=""></div>
      <span class="av-brand-sub">Interior condition report · IA</span>
    </div>
    <div class="av-bar-right">
      <span class="av-chip">Phenom 300</span>
      <span class="av-chip ai-chip"><span class="ai-dot"></span>Análise por IA</span>
      <span class="av-chip" id="prog-chip">0 / 24 itens</span>
    </div>
  </div>
  <div class="av-prog"><div class="av-prog-fill" id="prog-fill"></div></div>

  <div class="av-meta">
    <div class="av-meta-cell">
      <div class="av-meta-lbl">Aeronave</div>
      <input class="av-meta-in" id="meta-tail" type="text" value="PR-NGM" oninput="this.value=this.value.toUpperCase()">
    </div>
    <div class="av-meta-cell">
      <div class="av-meta-lbl">Data</div>
      <input class="av-meta-in" type="date" id="meta-date">
    </div>
    <div class="av-meta-cell">
      <div class="av-meta-lbl">Inspetor / Piloto</div>
      <input class="av-meta-in" type="text" placeholder="Nome completo">
    </div>
    <div class="av-meta-cell">
      <div class="av-meta-lbl">CANAC / CREA</div>
      <input class="av-meta-in" type="text" placeholder="Número de habilitação">
    </div>
  </div>

  <div class="av-body">
    <div class="av-ai-info">
      <i class="ti ti-robot" aria-hidden="true"></i>
      <p>Adicione até 7 fotos por item — a IA analisa cada imagem individualmente e consolida um score final com base em todas as perspectivas capturadas. Quanto mais fotos, mais precisa a avaliação.</p>
    </div>
  </div>

  <div class="av-body" id="av-form"></div>

  <div class="av-body">
    <div class="av-disc">
      <div class="av-disc-hdr">
        <i class="ti ti-alert-triangle" style="font-size:14px;color:var(--bold)" aria-hidden="true"></i>
        <span class="av-disc-title">Discrepâncias encontradas</span>
      </div>
      <div class="av-disc-body">
        <textarea class="av-disc-ta" rows="3" placeholder="Descreva as discrepâncias identificadas durante a inspeção..."></textarea>
        <div class="av-actions">
          <div class="av-act" onclick="this.classList.toggle('on')"><span class="dot"></span>Limpeza</div>
          <div class="av-act" onclick="this.classList.toggle('on')"><span class="dot"></span>Reparo</div>
          <div class="av-act" onclick="this.classList.toggle('on')"><span class="dot"></span>Pintura</div>
          <div class="av-act" onclick="this.classList.toggle('on')"><span class="dot"></span>Refresh de interior</div>
          <div class="av-act" onclick="this.classList.toggle('on')"><span class="dot"></span>Sem ação necessária</div>
        </div>
      </div>
    </div>

    <div class="av-sign">
      <div class="av-sign-hdr"><span class="av-sign-title">Encerramento e assinatura</span></div>
      <div class="av-sign-body">
        <div class="av-sign-fld"><label>Nome do inspetor / piloto</label><input class="av-sign-in" type="text" placeholder="Nome completo"></div>
        <div class="av-sign-fld"><label>CANAC / CREA</label><input class="av-sign-in" type="text" placeholder="Número"></div>
        <div class="av-canvas-wrap">
          <canvas id="sig-c" width="640" height="76"></canvas>
          <div class="av-canvas-ph" id="sig-ph">Assine aqui</div>
          <span class="av-canvas-clr" onclick="clearSig()">Limpar</span>
        </div>
      </div>
    </div>

    <div class="av-footer">
      <div class="av-summary">
        <div class="av-sum-item">
          <span class="av-sum-val" id="score-val">—</span>
          <span class="av-sum-lbl">Score geral</span>
        </div>
        <div class="av-sum-item">
          <span class="av-sum-cls" id="score-cls" style="color:var(--prem2)">—</span>
          <span class="av-sum-lbl">Classificação</span>
        </div>
        <div class="av-sum-item">
          <span class="av-sum-val" id="ai-count" style="font-size:18px;color:var(--ai)">0</span>
          <span class="av-sum-lbl">Fotos analisadas</span>
        </div>
      </div>
      <div class="av-footer-btns">
        <button class="av-submit sec" onclick="exportHTML()">
          <i class="ti ti-download" aria-hidden="true"></i>Baixar HTML
        </button>
        <button class="av-submit" onclick="finalize()">
          <i class="ti ti-file-check" aria-hidden="true"></i>Finalizar ↗
        </button>
      </div>
    </div>
  </div>
</div>

<script>
// LOGO
(function(){
  const svg=`data:image/svg+xml;utf8,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 220 80'><g transform='translate(18,4) scale(0.38)'><line x1='60' y1='0' x2='60' y2='120' stroke='white' stroke-width='10' stroke-linecap='round'/><line x1='0' y1='60' x2='120' y2='60' stroke='white' stroke-width='10' stroke-linecap='round'/><line x1='17' y1='17' x2='103' y2='103' stroke='white' stroke-width='10' stroke-linecap='round'/><line x1='103' y1='17' x2='17' y2='103' stroke='white' stroke-width='10' stroke-linecap='round'/></g><text x='68' y='52' font-family='Poppins,Arial,sans-serif' font-size='22' font-weight='600' fill='white' letter-spacing='6'>AVANTTO</text></svg>`;
  document.getElementById('av-logo').src=svg;
  const d=new Date();
  document.getElementById('meta-date').value=d.toISOString().split('T')[0];
})();

const ZONAS=[
  {id:'ck',num:'01',nome:'Cockpit',
   img:'https://upload.wikimedia.org/wikipedia/commons/thumb/8/8e/Embraer_Phenom_300_Cockpit.jpg/1280px-Embraer_Phenom_300_Cockpit.jpg',
   itens:[
    {id:'sp',nome:'Side panels',std:'Superfície uniforme, sem riscos, tom e textura constantes em toda a extensão.'},
    {id:'pl',nome:'Poltronas',std:'Couro íntegro, costuras rentes, braços sem desgaste visível, ajuste de recline suave.'},
    {id:'ci',nome:'Cintos de segurança',std:'Fitas limpas, sem desgaste, fecho com trava precisa e liberação eficiente.'},
    {id:'ca',nome:'Carpete',std:'Sem manchas ou fios soltos, especialmente junto aos pedais e trilhos de assento.'},
    {id:'qs',nome:'Quebra-sol',std:'Deslize suave, sem amarelamento, trincas ou deformações no material.'},
    {id:'hs',nome:'Headsets',std:'Espumas novas, cabo íntegro sem dobras, armazenado corretamente no suporte.'},
  ]},
  {id:'cb',num:'02',nome:'Cabine de passageiros',
   img:'https://www.privatefly.com/privatefly/assets/aircraft/photo/phenom300-interior-2.jpg',
   itens:[
    {id:'po',nome:'Poltronas',std:'Couro sem riscos ou manchas, recline suave, braços alinhados e sem desgaste.'},
    {id:'cs',nome:'Cintos de segurança',std:'Fitas claras e limpas, fecho firme sem folga excessiva.'},
    {id:'cr',nome:'Carpete',std:'Cor uniforme em toda área, sem zonas comprimidas, manchas ou fios expostos.'},
    {id:'pa',nome:'Passadeiras',std:'Fixadas ao carpete, sem dobras ou levantamento, padrão visual uniforme.'},
    {id:'pc',nome:'Porta-copos',std:'Mecanismo firme e suave, superfície sem arranhaduras ou marcas de uso.'},
    {id:'ms',nome:'Mesa de refeição / trabalho',std:'Superfície perfeitamente plana, travas funcionando, dobradiças sem folga ou rangido.'},
    {id:'sw',nome:'Sidewalls',std:'Revestimento homogêneo, sem amassados, bolhas ou pontos de descolamento.'},
    {id:'gl',nome:'Armários e galley',std:'Portas alinhadas, fechos firmes, interior limpo e sem odores.'},
  ]},
  {id:'lv',num:'03',nome:'Lavatory',
   img:'https://www.superiaircharter.com/wp-content/uploads/2018/07/Phenom300.jpg',
   itens:[
    {id:'pi',nome:'Piso',std:'Material antiderrapante íntegro, sem bolhas, levantamento ou pontos soltos.'},
    {id:'pr',nome:'Paredes',std:'Laminado sem manchas ou riscos, juntas perfeitamente vedadas.'},
    {id:'as',nome:'Assento sanitário',std:'Tampa alinhada, dobradiças firmes e silenciosas, material sem manchas ou trincas.'},
  ]},
  {id:'bg',num:'04',nome:'Bagageiro',
   img:'https://cdn.globalair.com/aircraft-for-sale/photos/1/2/1/1/2/1112198/55688_2.jpg',
   itens:[
    {id:'co',nome:'Compartimento geral',std:'Interior limpo, sem resíduos, manchas de óleo ou contaminantes.'},
    {id:'rv',nome:'Revestimento interno',std:'Fixado sem rasgos ou descascamento, cor uniforme, sem umidade.'},
    {id:'fx',nome:'Fixações e amarrações',std:'Velcro íntegro, ganchos e suportes firmes, sem pontos soltos.'},
  ]},
  {id:'sn',num:'05',nome:'Itens sensoriais',
   img:'https://www.privatefly.com/privatefly/assets/aircraft/photo/phenom300-interior-1.jpg',
   itens:[
    {id:'od',nome:'Odor da cabine',std:'Neutro, sem resíduos de alimento, combustível ou umidade.'},
    {id:'il',nome:'Iluminação',std:'Sem flicker, tom uniforme entre todos os pontos, sem lâmpadas queimadas.'},
    {id:'lp',nome:'Limpeza profunda geral',std:'Sem partículas visíveis em frestas, trilhos e rodapés.'},
  ]},
];

const LABELS=['Crítico','Ruim','Regular','Bom','Excelente'];
const MAX_PHOTOS=7;
const state={};
const photos={};
let totalAiCount=0;

// Per-item photo store: photos[zId][iId] = [{b64, score, obs}]

function build(){
  const f=document.getElementById('av-form');
  ZONAS.forEach(z=>{
    state[z.id]={};
    photos[z.id]={};
    const el=document.createElement('div');
    el.className='av-zone';
    el.innerHTML=`
      <div class="av-zone-hdr">
        <span class="av-zone-num">${z.num}</span>
        <span class="av-zone-name">${z.nome}</span>
        <span class="av-zone-badge">Score <span id="avg-${z.id}">—</span></span>
      </div>
      <div class="av-col-hdr">
        <div class="av-col-t">Estado atual · fotos + avaliação IA</div>
        <div class="av-col-t ideal">Padrão de referência · Phenom 300 novo</div>
      </div>`;
    z.itens.forEach((it,idx)=>{
      state[z.id][it.id]=null;
      photos[z.id][it.id]=[];
      const sc=[1,2,3,4,5].map(s=>
        `<button class="av-sb" onclick="setScore('${z.id}','${it.id}',${s},this)">${s}<br><span style="font-size:8px;opacity:.7">${LABELS[s-1]}</span></button>`
      ).join('');
      const itemEl=document.createElement('div');
      itemEl.className='av-item';
      itemEl.innerHTML=`
        <div class="av-item-grid">
          <div class="av-item-l">
            <div class="av-item-name">${it.nome}</div>
            <div class="av-scores" id="scores-${z.id}-${it.id}">${sc}</div>
            <div class="av-ref-lbl atual">Fotos do estado atual</div>
            <div class="av-gallery" id="gallery-${z.id}-${it.id}"></div>
            <input type="file" id="inp-${z.id}-${it.id}" accept="image/*" capture="environment" multiple style="display:none"
              onchange="handlePhotos(this,'${z.id}','${it.id}','${it.nome.replace(/'/g,"\\'")}','${it.std.replace(/'/g,"\\'")}')">
            <div class="av-ai-analyzing" id="analyzing-${z.id}-${it.id}">
              <i class="ti ti-loader-2"></i>
              <span id="analyzing-msg-${z.id}-${it.id}">Analisando foto...</span>
            </div>
            <div class="av-ai-result" id="ai-result-${z.id}-${it.id}">
              <div class="av-ai-result-hdr">
                <div class="av-ai-result-hdr-left">
                  <i class="ti ti-robot" aria-hidden="true"></i>
                  <span>Resultado da análise IA</span>
                </div>
                <span class="av-ai-photos-count" id="photo-count-${z.id}-${it.id}"></span>
              </div>
              <div class="av-ai-result-text" id="ai-text-${z.id}-${it.id}"></div>
              <div class="av-ai-score-row">
                <span class="av-ai-score-lbl">Score atribuído</span>
                <span class="av-ai-score-num" id="ai-score-num-${z.id}-${it.id}">—</span>
                <span class="av-ai-score-cat" id="ai-score-cat-${z.id}-${it.id}"></span>
                <span class="av-ai-reanalyze" onclick="reanalyze('${z.id}','${it.id}','${it.nome.replace(/'/g,"\\'")}','${it.std.replace(/'/g,"\\'")}')">↻ Reanalisar</span>
              </div>
            </div>
            <textarea class="av-obs" id="obs-${z.id}-${it.id}" placeholder="Observações do inspetor..." rows="2"></textarea>
          </div>
          <div class="av-item-r">
            <div class="av-ref-lbl ideal">Referência · saída de fábrica</div>
            ${idx===0?`
            <div class="av-ref-img-wrap">
              <img src="${z.img}" alt="Phenom 300 padrão" onerror="this.parentElement.style.display='none'">
              <div class="av-ref-badge">
                <span class="av-ref-badge-label">Phenom 300 · Novo</span>
                <span class="av-ref-badge-std">Padrão Embraer</span>
              </div>
              <div class="av-ref-corner"></div>
            </div>`:`<div style="height:28px;border-top:1px solid rgba(255,255,255,.04);margin-bottom:8px;display:flex;align-items:center;"><span style="font-size:9px;letter-spacing:1px;color:rgba(189,204,212,.25);text-transform:uppercase;">Ver imagem da zona acima</span></div>`}
            <div class="av-std-text">${it.std}</div>
          </div>
        </div>`;
      el.appendChild(itemEl);
    });
    const ftr=document.createElement('div');
    ftr.className='av-zone-ftr';
    ftr.innerHTML=`<span class="av-zone-ftr-lbl">Score médio da zona</span><span class="av-zone-ftr-val" id="avg-ftr-${z.id}">—</span>`;
    el.appendChild(ftr);
    f.appendChild(el);
    ZONAS.forEach(z=>z.itens.forEach(it=>renderGallery(z.id,it.id)));
  });
  recalc();
}

function renderGallery(zId,iId){
  const gallery=document.getElementById(`gallery-${zId}-${iId}`);
  if(!gallery)return;
  gallery.innerHTML='';
  const list=photos[zId][iId];
  list.forEach((p,idx)=>{
    const thumb=document.createElement('div');
    thumb.className='av-thumb filled';
    thumb.style.aspectRatio='1';
    const img=document.createElement('img');
    img.src='data:image/jpeg;base64,'+p.b64;
    img.style.cssText='width:100%;height:100%;object-fit:cover;display:block;';
    const del=document.createElement('div');
    del.className='thumb-del';
    del.innerHTML='<i class="ti ti-x" aria-hidden="true"></i>';
    del.onclick=e=>{e.stopPropagation();removePhoto(zId,iId,idx);};
    thumb.appendChild(img);
    thumb.appendChild(del);
    if(p.score){
      const badge=document.createElement('div');
      badge.className=`thumb-score s${p.score}`;
      badge.textContent=LABELS[p.score-1];
      thumb.appendChild(badge);
    } else if(p.analyzing){
      const spin=document.createElement('div');
      spin.className='thumb-spin';
      spin.innerHTML='<i class="ti ti-loader-2" aria-hidden="true"></i>';
      thumb.appendChild(spin);
    }
    gallery.appendChild(thumb);
  });
  if(list.length<MAX_PHOTOS){
    const add=document.createElement('div');
    add.className='av-thumb add-btn';
    add.style.aspectRatio='1';
    add.innerHTML=`<i class="ti ti-camera-plus" aria-hidden="true"></i><span class="thumb-lbl">Adicionar</span><span class="thumb-lbl-ai">IA analisa</span>`;
    add.onclick=()=>document.getElementById(`inp-${zId}-${iId}`).click();
    gallery.appendChild(add);
  }
}

async function handlePhotos(input,zId,iId,itemName,standard){
  if(!input.files||!input.files.length)return;
  const list=photos[zId][iId];
  const available=MAX_PHOTOS-list.length;
  const files=Array.from(input.files).slice(0,available);
  input.value='';
  for(const file of files){
    const b64=await fileToB64(file);
    list.push({b64,score:null,obs:'',analyzing:true});
    renderGallery(zId,iId);
    await analyzeOne(zId,iId,list.length-1,itemName,standard);
  }
  if(list.length>1) consolidate(zId,iId,itemName,standard);
}

function fileToB64(file){
  return new Promise(resolve=>{
    const r=new FileReader();
    r.onload=e=>resolve(e.target.result.split(',')[1]);
    r.readAsDataURL(file);
  });
}

async function analyzeOne(zId,iId,idx,itemName,standard){
  const list=photos[zId][iId];
  const p=list[idx];
  setAnalyzing(zId,iId,true,`Analisando foto ${idx+1}...`);
  try{
    const res=await callAI([{b64:p.b64}],itemName,standard,`foto ${idx+1} de ${list.length}`);
    p.score=res.score;
    p.obs=res.obs;
    p.analyzing=false;
    totalAiCount++;
    document.getElementById('ai-count').textContent=totalAiCount;
  }catch(e){
    p.analyzing=false;
    p.score=null;
  }
  renderGallery(zId,iId);
  setAnalyzing(zId,iId,false);
  updateResult(zId,iId);
}

async function consolidate(zId,iId,itemName,standard){
  const list=photos[zId][iId];
  if(list.length<2)return;
  setAnalyzing(zId,iId,true,`Consolidando ${list.length} fotos...`);
  try{
    const res=await callAI(list,itemName,standard,`conjunto de ${list.length} fotos`);
    document.getElementById(`ai-text-${zId}-${iId}`).textContent=res.obs;
    document.getElementById(`ai-score-num-${zId}-${iId}`).textContent=res.score+'.0';
    document.getElementById(`ai-score-cat-${zId}-${iId}`).textContent=LABELS[res.score-1];
    document.getElementById(`obs-${zId}-${iId}`).value=res.obs;
    document.getElementById(`photo-count-${zId}-${iId}`).textContent=list.length+' fotos';
    setScore(zId,iId,res.score,null);
    document.getElementById(`ai-result-${zId}-${iId}`).className='av-ai-result visible';
  }catch(e){console.error('consolidate error',e);}
  setAnalyzing(zId,iId,false);
}

function updateResult(zId,iId){
  const list=photos[zId][iId];
  if(!list.length)return;
  const last=list[list.length-1];
  if(!last.score)return;
  document.getElementById(`ai-text-${zId}-${iId}`).textContent=last.obs;
  document.getElementById(`ai-score-num-${zId}-${iId}`).textContent=last.score+'.0';
  document.getElementById(`ai-score-cat-${zId}-${iId}`).textContent=LABELS[last.score-1];
  document.getElementById(`obs-${zId}-${iId}`).value=last.obs;
  document.getElementById(`photo-count-${zId}-${iId}`).textContent=list.length+(list.length===1?' foto':' fotos');
  setScore(zId,iId,last.score,null);
  document.getElementById(`ai-result-${zId}-${iId}`).className='av-ai-result visible';
}

async function reanalyze(zId,iId,itemName,standard){
  const list=photos[zId][iId];
  if(!list.length)return;
  if(list.length===1) await analyzeOne(zId,iId,0,itemName,standard);
  else await consolidate(zId,iId,itemName,standard);
}

function removePhoto(zId,iId,idx){
  photos[zId][iId].splice(idx,1);
  renderGallery(zId,iId);
  const list=photos[zId][iId];
  if(!list.length){
    document.getElementById(`ai-result-${zId}-${iId}`).className='av-ai-result';
    state[zId][iId]=null;
    recalc();
  } else updateResult(zId,iId);
}

async function callAI(photoList,itemName,standard,context){
  const content=[];
  photoList.forEach((p,i)=>{
    content.push({type:'image',source:{type:'base64',media_type:'image/jpeg',data:p.b64}});
    content.push({type:'text',text:`Imagem ${i+1}:`});
  });
  const plural=photoList.length>1?'estas '+photoList.length+' fotos':'esta foto';
  content.push({type:'text',text:`Você é inspetor técnico especialista em interiors de aeronaves executivas, com foco em Embraer Phenom 300.

Analise ${plural} (${context}) do componente: "${itemName}"

Padrão de referência (aeronave nova de fábrica): "${standard}"

Com base em todas as imagens fornecidas, avalie o estado do componente e atribua:
1. Score de 1 a 5:
   1=Crítico (dano severo), 2=Ruim (reparo urgente), 3=Regular (atenção necessária), 4=Bom (pequenas imperfeições), 5=Excelente (como novo)
2. Observação técnica objetiva em português, máximo 2 frases: descreva o estado real observado e a ação recomendada se houver.

Responda SOMENTE em JSON válido sem texto adicional:
{"score": 4, "obs": "Observação aqui."}`});

  const res=await fetch('/api/claude',{
    method:'POST',
    headers:{'Content-Type':'application/json'},
    body:JSON.stringify({model:'claude-sonnet-4-6',max_tokens:1000,messages:[{role:'user',content}]})
  });
  if(!res.ok)throw new Error('API '+res.status);
  const data=await res.json();
  const text=data.content.filter(b=>b.type==='text').map(b=>b.text).join('');
  const parsed=JSON.parse(text.replace(/```json|```/g,'').trim());
  return{score:Math.min(5,Math.max(1,Math.round(parsed.score))),obs:parsed.obs||''};
}

function setAnalyzing(zId,iId,on,msg){
  const el=document.getElementById(`analyzing-${zId}-${iId}`);
  if(!el)return;
  el.className=on?'av-ai-analyzing visible':'av-ai-analyzing';
  if(msg) document.getElementById(`analyzing-msg-${zId}-${iId}`).textContent=msg;
}

function setScore(zId,iId,s,btn){
  state[zId][iId]=s;
  const container=document.getElementById(`scores-${zId}-${iId}`);
  if(container){
    container.querySelectorAll('.av-sb').forEach(b=>b.className='av-sb');
    if(btn){btn.className=`av-sb s${s}`;}
    else{const btns=container.querySelectorAll('.av-sb');if(btns[s-1])btns[s-1].className=`av-sb s${s}`;}
  }
  recalc();
}

function recalc(){
  let tot=0,cnt=0;
  ZONAS.forEach(z=>{
    const vs=Object.values(state[z.id]).filter(v=>v!==null);
    const avg=vs.length?(vs.reduce((a,b)=>a+b,0)/vs.length).toFixed(1):null;
    const a=document.getElementById('avg-'+z.id);if(a)a.textContent=avg||'—';
    const f=document.getElementById('avg-ftr-'+z.id);if(f)f.textContent=avg||'—';
    if(vs.length){tot+=vs.reduce((a,b)=>a+b,0);cnt+=vs.length;}
  });
  const total=ZONAS.reduce((a,z)=>a+z.itens.length,0);
  document.getElementById('prog-chip').textContent=cnt+' / '+total+' itens';
  document.getElementById('prog-fill').style.width=Math.round(cnt/total*100)+'%';
  if(cnt>0){
    const avg=tot/cnt;
    document.getElementById('score-val').textContent=avg.toFixed(1);
    const el=document.getElementById('score-cls');
    if(avg<=2){el.textContent='Reparo imediato';el.style.color='#CE5047';}
    else if(avg<=3){el.textContent='Limpeza profunda';el.style.color='#B9997D';}
    else if(avg<=4){el.textContent='Refresh programado';el.style.color='#E9A97A';}
    else{el.textContent='Conservado';el.style.color='#BDCCD4';}
  }
}

// SIGNATURE
let drawing=false,ctx,cnv;
function initSig(){
  cnv=document.getElementById('sig-c');
  ctx=cnv.getContext('2d');
  ctx.strokeStyle='#E9A97A';ctx.lineWidth=1.5;ctx.lineCap='round';
  const pos=e=>{const r=cnv.getBoundingClientRect();const sx=cnv.width/r.width,sy=cnv.height/r.height;const s=e.touches?e.touches[0]:e;return{x:(s.clientX-r.left)*sx,y:(s.clientY-r.top)*sy};};
  cnv.addEventListener('mousedown',e=>{drawing=true;const p=pos(e);ctx.beginPath();ctx.moveTo(p.x,p.y);document.getElementById('sig-ph').style.display='none';});
  cnv.addEventListener('mousemove',e=>{if(!drawing)return;const p=pos(e);ctx.lineTo(p.x,p.y);ctx.stroke();});
  cnv.addEventListener('mouseup',()=>drawing=false);
  cnv.addEventListener('touchstart',e=>{e.preventDefault();drawing=true;const p=pos(e);ctx.beginPath();ctx.moveTo(p.x,p.y);document.getElementById('sig-ph').style.display='none';},{passive:false});
  cnv.addEventListener('touchmove',e=>{e.preventDefault();if(!drawing)return;const p=pos(e);ctx.lineTo(p.x,p.y);ctx.stroke();},{passive:false});
  cnv.addEventListener('touchend',()=>drawing=false);
}
function clearSig(){if(ctx)ctx.clearRect(0,0,cnv.width,cnv.height);document.getElementById('sig-ph').style.display='flex';}

function exportHTML(){
  const blob=new Blob([document.documentElement.outerHTML],{type:'text/html'});
  const a=document.createElement('a');a.href=URL.createObjectURL(blob);
  const tail=document.getElementById('meta-tail').value||'aeronave';
  a.download=`relatorio_interior_${tail}_${new Date().toISOString().split('T')[0]}.html`;
  a.click();
}

function finalize(){
  const score=document.getElementById('score-val').textContent;
  const cls=document.getElementById('score-cls').textContent;
  const tail=document.getElementById('meta-tail').value;
  if(typeof sendPrompt==='function'){
    sendPrompt(`Inspeção interior ${tail} finalizada. Score geral: ${score}. Classificação: ${cls}. Total de fotos analisadas por IA: ${totalAiCount}. Qual o plano de manutenção recomendado?`);
  }else{
    alert(`Inspeção finalizada.\nAeronave: ${tail}\nScore: ${score}\nClassificação: ${cls}\nFotos analisadas: ${totalAiCount}`);
  }
}

build();
initSig();
</script>
</body>
</html>
